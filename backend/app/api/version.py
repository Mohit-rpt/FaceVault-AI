"""
Version API Router — Phase 2 Placeholder.

Will provide endpoints for:
- Model version tracking and management
- Version compatibility checks between server and edge devices
- Model update notifications

NOT registered in main.py until Phase 2 activation.
"""

from fastapi import APIRouter

router = APIRouter(prefix="/version", tags=["Version"])

# Phase 2: Version management endpoints will be defined here.
